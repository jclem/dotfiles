# Login shells load this before .zshrc; load it here for non-login shells too.
if [[ ! -o login && -r "$HOME/.zprofile" ]]; then
  source "$HOME/.zprofile"
fi

# Enable Atuin shell-history integration.
eval "$(atuin init zsh)"
